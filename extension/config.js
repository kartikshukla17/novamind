// Set your deployed app URL so the extension talks to production (e.g. Vercel).
// Use http://localhost:3000 for local development.
// eslint-disable-next-line no-var
var API_BASE_URL = "https://novamind-six.vercel.app/";
